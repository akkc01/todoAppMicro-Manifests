apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-del-task
  namespace: dev-todo-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dev-del-task
  template:
    metadata:
      labels:
        app: dev-del-task
    spec:
      containers:
      - name: dev-del-task
        image: akkc01/deletetasksmicro:fe91fb11f84617630fe69bba3f65e8a0cb713529
        ports:
        - containerPort: 8000
        env:
        - name: CONNECTION_STRING
          valueFrom:
            secretKeyRef:
              name: db-connection-secret
              key: CONNECTION_STRING

        resources:
          requests:
            memory: "128Mi"
            cpu: "250m"
          limits:
            memory: "256Mi"
            cpu: "500m"